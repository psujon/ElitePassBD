import { api } from './api';

// Dynamically initialize the Meta Pixel in browser (Disabled)
export const initPixel = () => {
  // Facebook Pixel disabled
};

/**
 * Fires duplicate Meta tracking events (Browser + Server-side Conversions API) - Disabled
 */
export const trackEvent = async (eventName, customData = {}, userData = {}, overrideEventId = null, skipServer = false) => {
  // Facebook Pixel disabled
};

